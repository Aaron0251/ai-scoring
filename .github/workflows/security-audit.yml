name: Security Audit

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]
  schedule:
    # 每週一 UTC 00:00 自動掃描
    - cron: '0 0 * * 1'

jobs:
  audit-frontend:
    name: Frontend npm audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Run npm audit
        working-directory: frontend
        # --audit-level=high 表示發現 high 或 critical 等級漏洞時才讓 CI 失敗
        # --omit=dev 排除僅建置期使用的 devDependencies（如 vite），因其不會被打包進正式容器（僅 dist/ 靜態檔案會被複製進 nginx 映像）
        run: npm audit --omit=dev --audit-level=high

  audit-backend:
    name: Backend npm audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: backend
        run: npm ci

      - name: Run npm audit
        working-directory: backend
        # --omit=dev 排除僅開發期使用的 devDependencies（如 prisma CLI），因 Dockerfile 以 npm ci --omit=dev 建置正式映像
        run: npm audit --omit=dev --audit-level=high
